#include <stdio.h>

int main() {
    int A, B, PROD;

    // Lê os dois números inteiros
    scanf("%d %d", &A, &B);

    // Calcula o produto
    PROD = A * B;

    // Exibe o resultado
    printf("PROD = %d\n", PROD);

    return 0;
}
