#include <stdio.h>

int main() {
    int A, B, X;

    // Lê os dois números inteiros
    scanf("%d %d", &A, &B);

    // Realiza a soma
    X = A + B;

    // Exibe o resultado
    printf("X = %d\n", X);

    return 0;
}
